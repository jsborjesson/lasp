#!/usr/bin/env ruby

require_relative "../lib/eval"

trap("SIGINT") { puts "\n\nBye!"; exit! }

loop do
  print "lasp> "
  input = gets.chomp

  result = Lasp::execute(input)
  puts "  =>> #{result}"
end
