#!/usr/bin/env ruby

require_relative "../lib/eval"

trap("SIGINT") { puts "\n\nBye!"; exit! }

loop do
  begin
    print "lasp> "
    input = gets.chomp

    result = Lasp::execute(input)
    puts "  =>> #{result}"
  rescue
    puts "  **> #{$!}"
  end
end
